services:
  server:
    container_name: nextjs-server
    image: registry.digitalocean.com/ib-registry/ib2-frontend
    pull_policy: always
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
    env_file:
      - .env
    networks:
      - nextjs
      
networks:
  nextjs:
    external: true